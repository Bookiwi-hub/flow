/**
 * @author 조현지
 * @description TypeScript 설정 파일
 * @date 2025-03-16
 */

{
  "extends": "../../tsconfig.next.json", // 상위 디렉토리에 있는 tsconfig.next.json 파일의 설정을 상속받는다.
  // TypeScript 컴파일러 옵션을 설정하는 객체
  "compilerOptions": {
    "experimentalDecorators": true // 데코레이터(@로 시작하는 문법) 사용을 허용하는 실험적 기능을 활성화한다.
  },
  "include": ["next-env.d.ts", "web.d.ts", "**/*.ts", "**/*.tsx"], // 컴파일에 포함할 파일 패턴을 지정한다.
  "exclude": ["node_modules"] // 컴파일에서 제외할 디렉토리를 지정한다.
}
