```json
{
  // "pipeline"은 Turborepo가 실행할 작업(Tasks)들을 정의하는 섹션입니다.
  // 여기서 정의된 작업들은 프로젝트의 패키지에서 실행되며, 의존성, 캐싱, 실행 방식 등을 설정할 수 있습니다.
  "pipeline": {
    // "build" 작업: 코드 빌드(컴파일, 번들링 등)를 담당하는 작업입니다.
    "build": {
      // "dependsOn": ["^build"] - 이 작업이 실행되기 전에 의존하는 다른 패키지의 "build" 작업이 먼저 완료되어야 함을 의미합니다.
      // "^build"에서 "^"는 현재 패키지가 의존하는 패키지들을 가리킵니다.
      // 예를 들어, 패키지 A가 패키지 B를 의존한다면, B의 build가 먼저 실행된 후 A의 build가 실행됩니다.
      "dependsOn": ["^build"],
      // "outputs": ["dist/**", ".next/**"] - 이 작업이 완료된 후 캐싱할 파일 또는 폴더를 지정합니다.
      // "dist/**"는 dist 폴더와 그 안의 모든 파일을 캐싱합니다 (일반적으로 빌드된 결과물).
      // ".next/**"는 Next.js 프로젝트에서 사용되는 빌드 출력 폴더를 캐싱합니다.
      // 캐싱을 통해 다음 실행 시 변경된 부분만 다시 빌드하여 시간을 절약할 수 있습니다.
      "outputs": ["dist/**", ".next/**"]
    },
    // "lint" 작업: 코드의 문법이나 스타일을 검사하는 작업입니다.
    "lint": {
      // "outputs": [] - lint 작업은 파일을 생성하지 않으므로 캐싱할 출력물이 없습니다.
      // 빈 배열을 설정하여 캐싱할 파일이 없음을 명시합니다.
      // Turborepo는 실행 로그를 캐싱하여 다음 실행 시 참고할 수 있습니다.
      "outputs": []
    },
    // "dev" 작업: 개발 모드에서 사용되는 작업입니다 (예: 개발 서버 실행).
    "dev": {}
    // 이 작업에는 추가 설정이 없습니다. 기본적으로 Turborepo의 기본 동작을 따릅니다.
    // 일반적으로 "dev" 작업은 지속적으로 실행되므로, 캐싱을 비활성화하거나("cache": false),
    // 작업이 종료되지 않도록("persistent": true) 설정하는 경우가 많습니다.
    // 여기서는 별도 설정이 없으므로 기본적으로 캐싱이 활성화되어 있습니다.
  }
}
```
